<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dixon.leetcode">

    <application
        android:name=".core.base.HomeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon_rect"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_rect"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".fun.mddetail.MdDetailActivity"></activity>
        <activity android:name=".fun.md.MdListActivity" />
        <activity android:name=".fun.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fun.topicdetail.TopicDetailActivity" />
        <activity android:name=".fun.topics.TopicsActivity" />
    </application>

</manifest>